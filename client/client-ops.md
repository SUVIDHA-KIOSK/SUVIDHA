1.create a good folder structure for client side code and move all files accordingly
2.tailwind css alredy installed and setup done ,
3. create a login page with the context that have given in the backend and also create a home page and setup react router dom for navigation between these two pages , setup a profile page for getting all these data 
4.setup zustand for state management and create a auth store for managing the authentication state of the user and also create a user store for managing the user data and also create a api folder for making all the api calls to the backend and also create a utils folder for creating all the utility functions that we will be using in our application
5. create a api folder for making all the api calls to the backend and also create a utils folder for creating all the utility functions that we will be using in our application
6.Follow all the api standard principles that we have defined in the backend for making all the api calls to the backend and also create a utils folder for creating all the utility functions that we will be using in our application
7.make all connections to env based and modularised one .
